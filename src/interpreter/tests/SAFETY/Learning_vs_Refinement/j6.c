/*
	An example to demonstrate that Blast traverses a
	larger tree than tracer. 

	The results are so far inconclusive.
*/

void main()
{
  int w, x, y, z, v;
  int a, b, a1, a2, a3, a4, a5, a6, a7, a8, a9;
  int a10, x101;

  x=0;
  y=0;
  z=0;
  w=0;
  v=0;

  a=0;
  while (a<100) {
    if (a1<0) b++; else b+=2;
    if (a2<0) b++; else b+=2;
    if (a3<0) b++; else b+=2;
    if (a4<0) b++; else b+=2;
    if (a5<0) b++; else b+=2;
    if (a6<0) b++; else b+=2;
    if (a7<0) b++; else b+=2;
    if (a8<0) b++; else b+=2;
    if (a9<0) b++; else b+=2;
    if (a10<0) b++; else b+=2;
    a++;

  if (x>0) {
  ERROR: goto ERROR;
  }

  }

/*     if (y>0) { */
/*       goto ERROR; */
/*     } */

/*     if (w>0) { */
/*       goto ERROR; */
/*     } */
/*     if (v>0) { */
/*       goto ERROR; */
/*     } */

    if (z>0) {
      goto ERROR;
    }



}
