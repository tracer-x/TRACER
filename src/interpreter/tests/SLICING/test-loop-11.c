// This example is to test loop invariants generated by other means.
// For instance, if we run octagons the we can infer [A=5, N=100,
// S-I>=0, I>=0, I<=100] producing an empty slice.
void main(){
  int i,s,N,a,z;

  i=0;
  s=0;
  N = 100;
  a =5;
  while (i < N){
    i++;
    s = s +i;  
  }
  if (s < 5)
    z = a  ;
  
  _SLICE(z);
  
}
