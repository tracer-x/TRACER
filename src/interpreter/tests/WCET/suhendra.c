
int main()
{
  int x, y, z, flag;

  if (x>3)
    z = z + 1;
  else
    x = flag;
  if (y==4)
    y = y + 1;
  else
    x = 1;
  if (x<2)
    z = z / 2;
  else
    z = z - 1;
  y = y + x - z;
  if (y > 0)
    z = x + y;
  else
    z = -1;
  return z;
}
